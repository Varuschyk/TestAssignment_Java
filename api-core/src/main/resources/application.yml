server:
  servlet:
    context-path: /api

spring:
  h2:
    console:
      enabled: true
  #Datasource settings
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:devDB
    username: admin
    password: admin
